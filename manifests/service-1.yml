apiVersion: v1
kind: Service
metadata:
    name: "timorcms"
    labels:
        app: "timorcms"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "timorcms"