apiVersion: v1
kind: Service
metadata:
    name: "timorcms"
    labels:
        app: "timorcms"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "timorcms"